; Minimal valid queues.conf to satisfy Asterisk module loader
[general]
persistentmembers = yes

; Example queue (no members by default)
[default]
musicclass = default
timeout = 15
retry = 5
wrapuptime = 0
strategy = ringall

